{
  "name": "amazon",
  "version": "1.0.0",
  "description": "video 4\r 1.Introduction 2. Install Tools 3. Create React-App\r 4.Create Git Repository",
  "main": "index.js",
  "scripts": {
    "build": "cd backend && npm install  && cd ../frontend && npm install && npm run build",
    "start": "node backend/server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  
  },
  "author": "",
  "license": "ISC"
}
