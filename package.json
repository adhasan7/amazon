{
  "name": "amazon",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "npm start --prefix backend",
    "build": "npm-run-all build:backend build:frontend",
    "dev:frontend": "cp .env frontend/.env; npm run dev --prefix frontend",
    "dev:backend": "npm run dev --prefix backend",
    "dev": "npm-run-all -p -r dev:backend dev:frontend",
    "install:frontend": "cp .env frontend/.env; npm install --prefix frontend",
    "install:backend": "npm install --prefix backend",
    "install": "npm-run-all install:backend install:frontend"

    "build:client": "cp .env client/.env; npm run build --prefix client",
    "build:server": "npm run build --prefix server && cp -r server/node_modules server/build/node_modules && node uninstaller.js",

    
    "install": "npm install --prefix frontend",
    "build": "npm run build --prefix frontend"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
    "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
